import{Router}from"express";import db from"../db";import{AuthRequest}from"../middleware/auth";const router=Router();const num=(v:any)=>Math.max(0,Number(v||0));
async function ensure(){await db.query(`CREATE TABLE IF NOT EXISTS loyalty_program_settings(id smallint PRIMARY KEY DEFAULT 1 CHECK(id=1),enabled boolean NOT NULL DEFAULT true,inactivity_days integer NOT NULL DEFAULT 360,points_enabled boolean NOT NULL DEFAULT true,updated_at timestamptz NOT NULL DEFAULT now(),updated_by text);INSERT INTO loyalty_program_settings(id) VALUES(1) ON CONFLICT DO NOTHING;CREATE TABLE IF NOT EXISTS loyalty_program_tiers(code text PRIMARY KEY,name text NOT NULL,color text NOT NULL,booked_threshold numeric(14,2) NOT NULL DEFAULT 0,paid_threshold numeric(14,2) NOT NULL DEFAULT 0,visits_threshold integer NOT NULL DEFAULT 0,discount_percent numeric(7,2) NOT NULL DEFAULT 0,sort_order integer NOT NULL,is_active boolean NOT NULL DEFAULT true,updated_at timestamptz NOT NULL DEFAULT now());INSERT INTO loyalty_program_tiers(code,name,color,booked_threshold,paid_threshold,visits_threshold,discount_percent,sort_order) VALUES('bronze','Bronz','#a66b3d',50000,50000,5,3,10),('silver','Ezüst','#8b95a1',150000,150000,12,5,20),('gold','Arany','#d5a51c',300000,300000,25,10,30) ON CONFLICT(code) DO NOTHING;CREATE TABLE IF NOT EXISTS loyalty_program_members(client_id uuid PRIMARY KEY REFERENCES clients(id) ON DELETE CASCADE,tier_code text REFERENCES loyalty_program_tiers(code),booked_total numeric(14,2) NOT NULL DEFAULT 0,paid_total numeric(14,2) NOT NULL DEFAULT 0,visit_count integer NOT NULL DEFAULT 0,last_visit_at timestamptz,evaluated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS loyalty_program_members_tier_idx ON loyalty_program_members(tier_code,evaluated_at DESC);`)}
router.get("/",async(_req,res,next)=>{try{await ensure();const[s,t,m,c]=await Promise.all([db.query(`SELECT * FROM loyalty_program_settings WHERE id=1`),db.query(`SELECT * FROM loyalty_program_tiers ORDER BY sort_order`),db.query(`SELECT pm.*,COALESCE(NULLIF(c.full_name,''),c.name,'Névtelen vendég') client_name,c.email,c.phone,t.name tier_name,t.color FROM loyalty_program_members pm JOIN clients c ON c.id=pm.client_id LEFT JOIN loyalty_program_tiers t ON t.code=pm.tier_code ORDER BY pm.evaluated_at DESC LIMIT 250`),db.query(`SELECT COALESCE(t.code,'none') code,COALESCE(t.name,'Nincs szint') name,COALESCE(t.color,'#9b948d') color,COUNT(pm.client_id)::int count FROM loyalty_program_members pm LEFT JOIN loyalty_program_tiers t ON t.code=pm.tier_code GROUP BY t.code,t.name,t.color ORDER BY MIN(t.sort_order) NULLS FIRST`)]);res.json({settings:s.rows[0],tiers:t.rows,members:m.rows,counts:c.rows})}catch(e){next(e)}});
router.put("/settings",async(req:AuthRequest,res,next)=>{try{await ensure();const{rows}=await db.query(`UPDATE loyalty_program_settings SET enabled=COALESCE($1,enabled),inactivity_days=COALESCE($2,inactivity_days),points_enabled=COALESCE($3,points_enabled),updated_at=now(),updated_by=$4 WHERE id=1 RETURNING *`,[req.body?.enabled,req.body?.inactivity_days,req.body?.points_enabled,req.user?.email||String(req.user?.id||"")]);res.json(rows[0])}catch(e){next(e)}});
router.put("/tiers",async(req,res,next)=>{const c=await db.connect();try{await ensure();const tiers=Array.isArray(req.body?.tiers)?req.body.tiers:[];if(!tiers.length)return res.status(400).json({message:"Legalább egy törzsvásárlói szint szükséges."});await c.query("BEGIN");for(const[tier,index]of tiers.entries())await c.query(`INSERT INTO loyalty_program_tiers(code,name,color,booked_threshold,paid_threshold,visits_threshold,discount_percent,sort_order,is_active,updated_at) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9,now()) ON CONFLICT(code) DO UPDATE SET name=EXCLUDED.name,color=EXCLUDED.color,booked_threshold=EXCLUDED.booked_threshold,paid_threshold=EXCLUDED.paid_threshold,visits_threshold=EXCLUDED.visits_threshold,discount_percent=EXCLUDED.discount_percent,sort_order=EXCLUDED.sort_order,is_active=EXCLUDED.is_active,updated_at=now()`,[String(tier.code||"").trim().toLowerCase(),String(tier.name||"").trim(),tier.color||"#8b95a1",num(tier.booked_threshold),num(tier.paid_threshold),Math.round(num(tier.visits_threshold)),num(tier.discount_percent),(index+1)*10,tier.is_active!==false]);await c.query("COMMIT");res.json({ok:true})}catch(e){await c.query("ROLLBACK");next(e)}finally{c.release()}});
router.post("/recalculate",async(_req,res,next)=>{const c=await db.connect();try{await ensure();await c.query("BEGIN");const settings=(await c.query(`SELECT * FROM loyalty_program_settings WHERE id=1`)).rows[0],tiers=(await c.query(`SELECT * FROM loyalty_program_tiers WHERE is_active ORDER BY sort_order DESC`)).rows,clients=(await c.query(`SELECT id,COALESCE(altegio_spent,0)::numeric booked_total,COALESCE(altegio_paid,altegio_spent,0)::numeric paid_total,COALESCE(altegio_visits,0)::int visit_count,altegio_last_visit last_visit_at FROM clients WHERE COALESCE(is_active,true)`)).rows;for(const client of clients){const inactive=client.last_visit_at&&new Date(client.last_visit_at).getTime()<Date.now()-Number(settings.inactivity_days||360)*86400000;const tier=inactive?null:tiers.find((t:any)=>(num(t.booked_threshold)>0&&num(client.booked_total)>=num(t.booked_threshold))||(num(t.paid_threshold)>0&&num(client.paid_total)>=num(t.paid_threshold))||(num(t.visits_threshold)>0&&num(client.visit_count)>=num(t.visits_threshold)));await c.query(`INSERT INTO loyalty_program_members(client_id,tier_code,booked_total,paid_total,visit_count,last_visit_at,evaluated_at) VALUES($1,$2,$3,$4,$5,$6,now()) ON CONFLICT(client_id) DO UPDATE SET tier_code=EXCLUDED.tier_code,booked_total=EXCLUDED.booked_total,paid_total=EXCLUDED.paid_total,visit_count=EXCLUDED.visit_count,last_visit_at=EXCLUDED.last_visit_at,evaluated_at=now()`,[client.id,tier?.code||null,client.booked_total,client.paid_total,client.visit_count,client.last_visit_at])}await c.query("COMMIT");res.json({ok:true,evaluated:clients.length})}catch(e){await c.query("ROLLBACK");next(e)}finally{c.release()}});export default router;
