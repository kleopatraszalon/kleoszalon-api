const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.resolve(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
const route=read('src/routes/cashierAltegioStage13.ts'),context=read('src/routes/cashierCheckoutContext.ts'),migration=read('src/sql/20260813_CASHIER_ALTEGIO_STAGE13.sql'),transactions=read('src/routes/transactions.ts'),fast=read('src/routes/workOrderCashierFast.ts'),financeAltegio=read('src/routes/financeAltegio.ts');
test('Stage13 supports multiple cash and cashless registers',()=>{assert.match(migration,/register_type varchar\(20\)/);assert.match(migration,/uq_cash_registers_location_name/);assert.match(migration,/uq_cash_register_one_open_session/);assert.match(route,/router\.get\('\/registers'/);assert.match(route,/router\.post\('\/registers'/);assert.match(route,/router\.patch\('\/registers\/:id'/)});
test('cashier supports denomination count interim check cashier handover and dated history',()=>{assert.match(migration,/CREATE TABLE IF NOT EXISTS cash_register_counts/);assert.match(migration,/count_type IN\('opening','check','handover','closing'\)/);assert.match(route,/router\.post\('\/sessions\/:id\/check'/);assert.match(route,/Pénztárátadásnál adja meg az átvevő pénztárost/);assert.match(route,/handover_from_session_id/);assert.match(context,/router\.get\('\/register-history'/);assert.match(context,/previous_closing_count/)});
test('register transfers and extended manual income expense are audited',()=>{assert.match(migration,/CREATE TABLE IF NOT EXISTS cash_register_transfers/);assert.match(route,/router\.post\('\/transfers'/);assert.match(route,/movement_type/);assert.match(route,/reference_no/);assert.match(route,/partner_id/);assert.match(route,/employee_id/)});
test('cash cannot bypass an open register even through fallback or loyalty checkout',()=>{assert.match(migration,/CREATE OR REPLACE FUNCTION enforce_cash_payment_open_register/);assert.match(migration,/Készpénzes fizetés előtt nyissa meg a pénztárt/);assert.match(migration,/CREATE TRIGGER trg_work_order_cash_payment_register/);assert.match(migration,/CREATE TABLE IF NOT EXISTS cashier_checkout_context/);assert.match(context,/cashier_checkout_context/);assert.ok(transactions.indexOf('cashierAltegioStage13Router')<transactions.indexOf('workOrderCashierFastRouter'));assert.match(fast,/register_session_id/)});
test('partial and full refunds remain UUID-linked auditable and affect net paid total',()=>{assert.match(migration,/CREATE TABLE IF NOT EXISTS work_order_payment_refunds/);assert.match(migration,/payment_id uuid NOT NULL REFERENCES work_order_payments\(id\)/);assert.match(migration,/refunded_amount numeric\(14,2\)/);assert.match(route,/router\.post\('\/payments\/:paymentId\/refund'/);assert.match(route,/amount-COALESCE\(refunded_amount,0\)/);assert.match(fast,/amount-COALESCE\(refunded_amount,0\)/)});
test('existing Altegio finance parity remains connected for categories partners methods fees and cancellation',()=>{for(const marker of['finance_partners','finance_document_types','finance_payment_methods','fee_percent','fee_fixed','brand_fees',"router.post('/operations/:id/cancel'"])assert.ok(financeAltegio.includes(marker),`missing ${marker}`);assert.match(route,/router\.get\('\/payment-methods'/);assert.match(route,/card_brand/);assert.match(route,/fee_amount/)});
