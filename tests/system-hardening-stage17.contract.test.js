const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const read=p=>fs.readFileSync(path.join(process.cwd(),p),'utf8');
test('Stage17 record lifecycle is admin-only and reversible',()=>{const facade=read('src/routes/clients.ts'),route=read('src/routes/systemHardening.ts'),life=read('src/routes/recordLifecycle.ts');assert.match(facade,/system-hardening/);assert.match(route,/recordLifecycle/);assert.match(life,/requireAdmin/);assert.match(life,/deleted_at/);assert.match(life,/deleted_by/);assert.match(life,/delete_reason/);assert.match(life,/\/:entity\/:id\/archive/);assert.match(life,/\/:entity\/:id\/restore/);assert.doesNotMatch(life,/router\.delete|r\.delete/);assert.match(life,/action:\"soft_delete\"/);assert.match(life,/action:\"restore\"/)});
test('Stage17 audit writer redacts credentials and records request context',()=>{const src=read('src/audit/systemAudit.ts');assert.match(src,/SENSITIVE_KEY/);assert.match(src,/password/);assert.match(src,/authorization/);assert.match(src,/\[redacted\]/);assert.match(src,/request_id/);assert.match(src,/ip_address/);assert.match(src,/user_agent/);assert.match(src,/x-forwarded-for/);assert.match(src,/writeSystemAudit/)});
