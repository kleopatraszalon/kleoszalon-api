name: P10 route one-shot patch
on:
  push:
    branches: [feature/vir-p10-revenue-autopilot]
    paths: ['.github/workflows/p10-route-one-shot.yml']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {ref: feature/vir-p10-revenue-autopilot}
      - run: node scripts/apply-p10-route-patch.cjs
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'feat: mount VIR P10 revenue autopilot'
          git push origin HEAD:feature/vir-p10-revenue-autopilot
