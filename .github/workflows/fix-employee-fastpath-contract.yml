name: Fix employee fast-path contract

on:
  push:
    branches:
      - agent/fix-dashboard-runtime-500s
    paths:
      - scripts/fix-employee-fastpath-contract.mjs
      - .github/workflows/fix-employee-fastpath-contract.yml

permissions:
  contents: write

jobs:
  fix-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-dashboard-runtime-500s
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Apply employee fast-path repair
        run: node scripts/fix-employee-fastpath-contract.mjs
      - name: Install dependencies
        run: npm ci --include=dev --no-audit --no-fund
      - name: Run employee first-request contract
        run: node --test tests/first-request-workorder-readiness.contract.test.js
      - name: Run full regression suite
        run: npm test
      - name: TypeScript backend build
        run: |
          npm run clean
          npx tsc -p tsconfig.server.json
      - name: Commit validated source and remove one-shot assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/employees.ts
          git rm scripts/fix-employee-fastpath-contract.mjs
          git rm .github/workflows/fix-employee-fastpath-contract.yml
          git diff --cached --check
          git commit -m "fix: preserve employee no-DDL fast path"
          git push origin HEAD:agent/fix-dashboard-runtime-500s
